import { useState} from "react";
import Section from '../../base/Section'
import './index.css';

const Work = (props) => {
    return (
        <Section {...props}>
            <p>Work Section</p>
        </Section>
    )
}
export default Work;